[tasks.build]
workspace = false
command = "cargo"
args = ["build"]

[tasks.example]
workspace = false
dependencies = ["build"]
script = '''
cd target/debug && ./moon render --once --html=../../fixtures/test.html --css=../../fixtures/test.css --size=500x300 --output=../../image.png

'''

[tasks.try]
workspace = false
dependencies = ["build"]
script = '''
cd target/debug && ./moon render --once --html=../../fixtures/${@}.html --css=../../fixtures/${@}.css --size=500x300 --output=../../image.png

'''

[tasks.output]
workspace = false
dependencies = ["build"]
script = '''
cd target/debug && ./moon render --once --html=../../fixtures/${@}.html --css=../../fixtures/${@}.css --size=500x300 --output=../../image.png
'''
